export type IDirection = 'left' | 'right' | 'top' | 'down';

export type ISnakePosition = { top: number; left: number };

export type IFood = { rowFood: number; columnFood: number };

export type IHighscore = { date: Date; score: number };
